name: License Compliance

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Mondays at 9am UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # The gate is defined once in the fleet-shared repo; this repo only wires it.
  # To change license policy for the fleet, edit that workflow, not this file.
  license-check:
    uses: MustardSeedNetworks/.github/.github/workflows/license-check.yml@cd60ad04e0f305b3706481580091dc7f3e1863aa # v1.11.0
